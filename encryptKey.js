const ethers = require("ethers");
const fs = require("fs");
require("dotenv").config(); 

async function main() {}
main()
  .then(() => process.exitCode(0))
  .catch((error) => {
    console.log(error);
    console.error(error);
  });
